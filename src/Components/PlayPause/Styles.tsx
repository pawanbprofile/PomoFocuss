import {StyleSheet} from 'react-native';
import Sizes from '../../Utils/Sizes';

const Styles = StyleSheet.create({
  container: {
    width: '100%',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: Sizes.Padding.XXXXL,
  },
});

export default Styles;
